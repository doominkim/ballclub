#!/usr/bin/env bash
# Ballclub launcher
set -euo pipefail

if ! command -v codex >/dev/null 2>&1; then
  printf '%s\n' "ballclub: Codex CLI was not found on PATH" >&2
  exit 127
fi

case "${1:-}" in
  -h|--help|-V|--version)
    exec codex "$@"
    ;;
esac

exec codex "$@"
