interface:
  display_name: "Calling Multiple Players"
  short_description: "Set independent plate appearances in parallel."
  default_prompt: "Send multiple players to bat only for independent work or context isolation, then return compact evidence for official scoring."
