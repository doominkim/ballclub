interface:
  display_name: "Capacity Routing"
  short_description: "Choose a capacity profile before delegation."
  default_prompt: "Classify this task and select a suitable available capacity profile before delegating."
